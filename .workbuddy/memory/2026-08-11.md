# 2026-08-11

## 构建：AI 带货短视频脚本引擎（Vite+React18+Tailwind）

用户基于《AI带货短视频内容生产系统·智能脚本引擎升级方案》要求做成一个网站。
按实施优先级，第一优先级做扎实，趋势/效果做可用骨架。

- 工程：`/Users/qinaqiang/WorkBuddy/2026-08-11-22-33-10/` (Vite+React+Tailwind)
- DeepSeek 经 Vite dev-server proxy `/api/deepseek` 转发，规避浏览器 CORS；Key 存 localStorage
- 已实现：商品痛点地图(LLM)、维度组合生成(5维加权采样+多样性避重)、人物场景生成(LLM)、时长/渠道参数化、一键复制导出(人类可读/JSON/Seedance2/MiniMax H3)、趋势结构指纹录入+聚合、效果分发记录→维度权重更新
- 验证：npm install + npm run build 通过(44模块)，dev 服务 200
- 后续：趋势情报接真实合规数据源(蝉妈妈/TikTok Creative Center API)；视频模型精确输入格式需对齐官方文档

## 追加：视频模型精确导出（联网核实真实 API）
- 新增 `src/lib/videoModels.js` + `src/components/VideoExport.jsx`
- Seedance 2.0：`{model:"seedance-2.0", input:{prompt, ratio, duration, resolution}}`，ratio∈16:9/9:16/4:3/1:1/3:4/21:9，duration 4-15s，支持@Image多模态参考
- MiniMax Hailuo：`POST /v1/video_generation {model:"MiniMax-Hailuo-2.3", prompt, duration, resolution, prompt_optimizer}`，prompt 内 [Push in]/[Pan left]/[Static shot] 等运镜指令
- 两种模式：逐镜生成(每镜一个请求体) / 整片合成(单调用，受时长上限)；产出精确请求体 JSON + 可填端点/Key 的 curl 模板
- 旧文本式 seedance/minimax 适配器已移除（被 VideoExport 取代）；导出下拉仅保留 人类可读/JSON
- 验证：npm run build 通过(46模块)
